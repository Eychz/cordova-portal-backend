# Backend Environment Variables Template
# Copy this file to .env and fill in your actual values

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
# PostgreSQL Connection String (Use your Neon PostgreSQL URL)
DATABASE_URL="postgresql://username:password@host:5432/database_name?sslmode=require"

# Example Neon URL format:
# DATABASE_URL="postgresql://cordova_user:your_password@ep-xxx-xxx.us-east-2.aws.neon.tech/cordova_db?sslmode=require"

# ===========================================
# JWT AUTHENTICATION
# ===========================================
# Secret key for JWT tokens (use a strong random string)
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"

# ===========================================
# SERVER CONFIGURATION
# ===========================================
# Port for the backend server
PORT=5000

# Node environment (development, production, test)
NODE_ENV=development

# ===========================================
# FRONTEND CONFIGURATION
# ===========================================
# Frontend URL for CORS (add your deployed frontend URL)
FRONTEND_URL="http://localhost:3000"
# Production example:
# FRONTEND_URL="https://cordova-municipality-portal.vercel.app"

# ===========================================
# EMAIL CONFIGURATION (Gmail)
# ===========================================
# Gmail account for sending verification emails
EMAIL_USER="your-email@gmail.com"

# Gmail App Password (not your regular password)
# Generate at: https://myaccount.google.com/apppasswords
EMAIL_PASS="your-gmail-app-specific-password"

# Email sender name
EMAIL_FROM="Cordova Municipality Portal <your-email@gmail.com>"

# ===========================================
# GOOGLE CLOUD STORAGE (Optional - Not Used Currently)
# ===========================================
# GCS_BUCKET_NAME="your-bucket-name"
# GCS_PROJECT_ID="your-project-id"
# GCS_KEY_FILE="path/to/service-account-key.json"

# ===========================================
# DEPLOYMENT NOTES
# ===========================================
# For Render.com deployment:
# 1. Add all these variables in the Render dashboard
# 2. Use Internal Database URL if using Render PostgreSQL
# 3. Set NODE_ENV=production
# 4. Update FRONTEND_URL to your Vercel deployment URL

# For Railway deployment:
# 1. Add variables in Railway dashboard
# 2. Use Railway's PostgreSQL plugin for DATABASE_URL
# 3. Set NODE_ENV=production
